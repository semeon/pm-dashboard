function DataModel(userSettings, appSettings, redmineSettings) {

  self = this;

  this.projectList = {};

  // for (var p=0; p<userSettings.projects.length; p++) {

  //   var project = {};
  //   project.id = userSettings.projects[p].id;
  // 	self.projectList[project.id] = project;

		// // loadProjectData(project);

  // }



	// PRIVATE
	// -------------------------------------------------------------------------------------------



}